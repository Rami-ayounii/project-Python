{
  "name": "flask-employee-management",
  "private": true,
  "scripts": {
    "start": "python3 -m ensurepip && python3 -m pip install -r requirements.txt && python3 app.py",
    "dev": "python3 -m ensurepip && python3 -m pip install -r requirements.txt && python3 app.py"
  }
}